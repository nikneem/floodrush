{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Flip to true to target the Android emulator (requires devtunnel CLI and login).
  // Leave false (the default) to run the Windows desktop client instead.
  "UseAndroidEmulator": false
}
