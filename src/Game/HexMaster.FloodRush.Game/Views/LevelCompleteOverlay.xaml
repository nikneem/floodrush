<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HexMaster.FloodRush.Game.Views.LevelCompleteOverlay">
    <Grid>
        <!-- Full-bleed celebration visual -->
        <Image Source="level_completed_visual.png"
               Aspect="AspectFill"
               Opacity="0.92"/>
        <!-- Subtle dark veil so the card stays readable -->
        <BoxView Color="{StaticResource AppBackground}" Opacity="0.45"/>

        <!-- Centred celebration card -->
        <Border Style="{StaticResource GameCardStyle}"
                HorizontalOptions="Center"
                VerticalOptions="Center"
                MinimumWidthRequest="360"
                MaximumWidthRequest="520">
            <VerticalStackLayout Spacing="20" Padding="8">

                <!-- Trophy icon -->
                <Label Text="🏆"
                       FontSize="64"
                       HorizontalTextAlignment="Center"/>

                <!-- Heading -->
                <Label Text="Level Complete!"
                       Style="{StaticResource PageTitleStyle}"
                       TextColor="{StaticResource Success}"
                       HorizontalTextAlignment="Center"/>

                <!-- Score earned -->
                <VerticalStackLayout Spacing="4" HorizontalOptions="Center">
                    <Label Text="You scored"
                           Style="{StaticResource MutedTextStyle}"
                           HorizontalTextAlignment="Center"/>
                    <Label HorizontalTextAlignment="Center">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="{Binding Score}"
                                      FontFamily="Peralta"
                                      FontSize="48"
                                      TextColor="{StaticResource BrandAmber}"/>
                                <Span Text=" pts"
                                      FontFamily="PatrickHand"
                                      FontSize="22"
                                      TextColor="{StaticResource TextSecondary}"/>
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>
                </VerticalStackLayout>

                <!-- Score breakdown -->
                <Grid ColumnDefinitions="*,Auto"
                      RowSpacing="4"
                      ColumnSpacing="12">

                    <!-- Pipe score -->
                    <Label Grid.Row="0" Grid.Column="0"
                           Text="Pipe score"
                           Style="{StaticResource MutedTextStyle}"/>
                    <Label Grid.Row="0" Grid.Column="1"
                           Text="{Binding ScoreBreakdownPipeScore, StringFormat='+{0} pts'}"
                           FontFamily="PatrickHand"
                           FontSize="16"
                           TextColor="{StaticResource TextPrimary}"
                           HorizontalTextAlignment="End"/>

                    <!-- Basin bonus (hidden when 0) -->
                    <Label Grid.Row="1" Grid.Column="0"
                           Text="Basin bonus"
                           Style="{StaticResource MutedTextStyle}"
                           IsVisible="{Binding ScoreBreakdownBasinBonus, Converter={StaticResource IsNotZeroConverter}}"/>
                    <Label Grid.Row="1" Grid.Column="1"
                           Text="{Binding ScoreBreakdownBasinBonus, StringFormat='+{0} pts'}"
                           FontFamily="PatrickHand"
                           FontSize="16"
                           TextColor="{StaticResource Success}"
                           HorizontalTextAlignment="End"
                           IsVisible="{Binding ScoreBreakdownBasinBonus, Converter={StaticResource IsNotZeroConverter}}"/>

                    <!-- Split bonus (hidden when 0) -->
                    <Label Grid.Row="2" Grid.Column="0"
                           Text="Split bonus"
                           Style="{StaticResource MutedTextStyle}"
                           IsVisible="{Binding ScoreBreakdownSplitBonus, Converter={StaticResource IsNotZeroConverter}}"/>
                    <Label Grid.Row="2" Grid.Column="1"
                           Text="{Binding ScoreBreakdownSplitBonus, StringFormat='+{0} pts'}"
                           FontFamily="PatrickHand"
                           FontSize="16"
                           TextColor="{StaticResource Success}"
                           HorizontalTextAlignment="End"
                           IsVisible="{Binding ScoreBreakdownSplitBonus, Converter={StaticResource IsNotZeroConverter}}"/>

                    <!-- Completion bonus -->
                    <Label Grid.Row="3" Grid.Column="0"
                           Text="Completion bonus"
                           Style="{StaticResource MutedTextStyle}"/>
                    <Label Grid.Row="3" Grid.Column="1"
                           Text="{Binding ScoreBreakdownCompletionBonus, StringFormat='+{0} pts'}"
                           FontFamily="PatrickHand"
                           FontSize="16"
                           TextColor="{StaticResource Success}"
                           HorizontalTextAlignment="End"/>

                    <!-- Unused pipe penalty (hidden when 0, red) -->
                    <Label Grid.Row="4" Grid.Column="0"
                           Text="Unused pipes"
                           Style="{StaticResource MutedTextStyle}"
                           TextColor="{StaticResource Danger}"
                           IsVisible="{Binding ScoreBreakdownUnusedPipePenalty, Converter={StaticResource IsNotZeroConverter}}"/>
                    <Label Grid.Row="4" Grid.Column="1"
                           Text="{Binding ScoreBreakdownUnusedPipePenalty, StringFormat='{0} pts'}"
                           FontFamily="PatrickHand"
                           FontSize="16"
                           TextColor="{StaticResource Danger}"
                           HorizontalTextAlignment="End"
                           IsVisible="{Binding ScoreBreakdownUnusedPipePenalty, Converter={StaticResource IsNotZeroConverter}}"/>

                    <!-- Divider -->
                    <BoxView Grid.Row="5" Grid.ColumnSpan="2"
                             Color="{StaticResource SurfaceSecondary}"
                             HeightRequest="1"
                             Margin="0,4"/>

                    <!-- Total -->
                    <Label Grid.Row="6" Grid.Column="0"
                           Text="Total"
                           FontFamily="PatrickHand"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource TextPrimary}"/>
                    <Label Grid.Row="6" Grid.Column="1"
                           Text="{Binding ScoreBreakdownTotal, StringFormat='{0} pts'}"
                           FontFamily="PatrickHand"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{StaticResource BrandAmber}"
                           HorizontalTextAlignment="End"/>

                </Grid>

                <!-- Score-submission indicator -->
                <StackLayout Orientation="Horizontal"
                             HorizontalOptions="Center"
                             Spacing="8"
                             IsVisible="{Binding IsScoreSubmitting}">
                    <ActivityIndicator IsRunning="{Binding IsScoreSubmitting}"
                                       Color="{StaticResource BrandAmber}"
                                       WidthRequest="18"
                                       HeightRequest="18"/>
                    <Label Text="Saving score to server…"
                           Style="{StaticResource MutedTextStyle}"
                           VerticalOptions="Center"/>
                </StackLayout>

                <!-- Highscores -->
                <Grid ColumnDefinitions="*,1,*"
                      RowDefinitions="Auto,Auto,Auto"
                      ColumnSpacing="16"
                      RowSpacing="4">

                    <!-- Divider -->
                    <BoxView Grid.Row="0" Grid.ColumnSpan="3"
                             Color="{StaticResource SurfaceSecondary}"
                             HeightRequest="1"
                             Margin="0,4"/>

                    <!-- Column headers -->
                    <Label Grid.Row="1" Grid.Column="0"
                           Text="Your best"
                           Style="{StaticResource MutedTextStyle}"
                           HorizontalTextAlignment="Center"/>
                    <BoxView Grid.Row="1" Grid.Column="1"
                             Color="{StaticResource SurfaceSecondary}"
                             WidthRequest="1"/>
                    <Label Grid.Row="1" Grid.Column="2"
                           Text="Global best"
                           Style="{StaticResource MutedTextStyle}"
                           HorizontalTextAlignment="Center"/>

                    <!-- Values -->
                    <Label Grid.Row="2" Grid.Column="0"
                           Text="{Binding PlayerHighScore, StringFormat='{0:N0} pts', TargetNullValue='—'}"
                           FontFamily="PatrickHand"
                           FontSize="22"
                           TextColor="{StaticResource BrandAmber}"
                           HorizontalTextAlignment="Center"/>
                    <BoxView Grid.Row="2" Grid.Column="1"
                             Color="{StaticResource SurfaceSecondary}"
                             WidthRequest="1"/>
                    <Label Grid.Row="2" Grid.Column="2"
                           Text="{Binding GlobalHighScore, StringFormat='{0:N0} pts', TargetNullValue='—'}"
                           FontFamily="PatrickHand"
                           FontSize="22"
                           TextColor="{StaticResource TextPrimary}"
                           HorizontalTextAlignment="Center"/>

                </Grid>

                <!-- Actions -->
                <Button Text="Next Level"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding NextLevelCommand}"
                        HorizontalOptions="Fill"/>
                <Button Text="Quit to Menu"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding QuitCommand}"
                        HorizontalOptions="Fill"/>

            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentView>
